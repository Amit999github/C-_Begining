#include <iostream>

using namespace std;

int main(){

    // mutable variable means that value will be change or re initialise will be allowed 
    int num = 52343;
    cout << "immutable " << num << endl;
    // immutable variable means that value will be not change or re initialise will be not allowed
    const int num2 = 643653;
    cout << "immutable " << num2 << endl;

    cout << "welcome to chai or code me \n";
    cout << "hello world";
};